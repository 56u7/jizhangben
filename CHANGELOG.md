# 更新记录

## 2.39

- 新增四视图左右滑动切换（借出借入 / 明细 / 日历 / 统计），松手后的动效与点击顶部按钮一致
- 记一笔弹窗改为按内容自适应高度，不弹键盘时全部内容一屏显示
- 明细列表：选中整行淡蓝；批量操作支持右滑标记 / 左滑删除 / 长按不计入，并可按日期整组错峰补位
- 删除明细时背景卡片向中心收缩、下方卡片错峰上顶；删除后可撤销，撤销后不保留选中
- 新增搜索（备注 / 分类 / 金额，搜全部历史）
- 主题切换改为即时切换，不再有整屏明暗过渡
- 统计视图：月历对比卡去除重复的月份切换入口

## 2.10.1

- 添加预构建 APK 下载
- 修复 Gradle Wrapper 可执行权限

## 2.10

- 支持微信 / 支付宝账单文件自动识别导入
- 支持微信 / 支付宝支付通知及交易提醒捕获
- 新增待导入账目列表：修改、标星、删除、不计入预算
- 清理导入账单支持按来源和月份单独清理
- 修复今日剩余超额时归零显示和浅色 / 深色主题颜色
- 修复 CSV 文件在手机文件选择器中被置灰的问题
- 增加通知捕获状态自检

## 更早版本

早期版本为内部迭代版本，不单独列出。
